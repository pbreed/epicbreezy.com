# Local credentials -- never commit
.secrets/
*.token
*.pem
*.key

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.bak
.~lock.*

# Credentials that were sitting untracked and unignored -- `*.token` above does
# not match a file simply named `github_token`.
github_token
.git-credentials
